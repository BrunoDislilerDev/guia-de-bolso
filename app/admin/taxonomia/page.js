import TaxonomiaPage from "@/components/admin/TaxonomiaPage";

/**
 * Admin — taxonomia (subcategorias e tags).
 * @returns {import("react").JSX.Element}
 */
export default function AdminTaxonomiaRoute() {
  return <TaxonomiaPage />;
}
